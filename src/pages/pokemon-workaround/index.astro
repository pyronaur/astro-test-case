---
// Load your data with fetch(), Astro.fetchContent(), etc.
const response = await fetch(`https://pokeapi.co/api/v2/pokemon?limit=150`);
const result = await response.json();
const allPokemon = result.results;
const firstPage = allPokemon.slice(0, 10);
---

<strong>Pokemons:</strong>

<br>

{firstPage.map(p => {
    return `<a href="/pokemon-workaround/${p.name}">View ${p.name}</a><br>`;
})}

<br>

<a href="/pokemon-workaround/page/2">Go to next page</a>